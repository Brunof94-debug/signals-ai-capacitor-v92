{
  "name": "signals-ai-capacitor",
  "version": "9.3.0",
  "private": true,
  "description": "Capacitor Android wrapper for Signals AI PWA",
  "scripts": {
    "build:web": "echo \"(Opcional) gere sua pasta web aqui, se necessário\"",
    "cap:add:android": "npx cap add android",
    "cap:sync:android": "npx cap sync android",
    "android:debug": "cd android && ./gradlew assembleDebug",
    "android:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
