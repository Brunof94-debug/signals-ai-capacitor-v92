{
  "name": "signals-ai-capacitor",
  "version": "9.2.0",
  "private": true,
  "description": "Capacitor Android wrapper for Signals AI PWA with FCM push",
  "scripts": {
    "clean": "rimraf android",
    "sync:android": "npx cap sync android",
    "build:android": "cd android && ./gradlew assembleDebug",
    "android": "npm run sync:android && npm run build:android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/push-notifications": "^6.0.2"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
